streamlit==1.56.0
plotly==6.7.0
pandas==3.0.2
numpy==2.4.4
altair==6.1.0
pillow==12.2.0
requests==2.33.1
anthropic>=0.40.0
pyarrow==24.0.0
# Flag-issue body parsing (drilldown card fetches the BEGIN_FLAG_DATA YAML
# block from each open issue to render the proposed correction inline).
# Falls back to regex if pyyaml is unavailable, but proper parsing is robust.
pyyaml>=6.0
# Independent-LLM cross-validation harness (scripts/validate_independent_llm.py).
# Required by the validation loop's non-Anthropic providers — without these,
# a fresh checkout fails with ModuleNotFoundError when the script picks up
# a GEMINI_API_KEY or GROQ_API_KEY.
google-genai>=1.0.0
groq>=0.20.0
